import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nakama.ahora',
  appName: 'Ahora',
  webDir: 'dist',
  // Plugins se configurarán en fases posteriores (notificaciones, voz).
};

export default config;
